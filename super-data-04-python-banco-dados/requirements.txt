# requirements.txt tem todas as dependencias do projeto
# https://pypi.org/ centralizador de bibliotecas

# pip install rich

# Instalar todas as dependencias do requirements.txt
# py -m pip install -r requirements.txt
# pip install -r requirements.txt

rich==15.0.0
requests==2.34.2

# ver todas as dependencias instaladas
# pip freeze